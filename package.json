{
  "name": "iab-taxonomy-classifier",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "seed-taxonomy": "tsx scripts/seed-taxonomy.ts",
    "generate:synthetic": "tsx scripts/generate-synthetic-data.ts",
    "clean:synthetic": "rm -f data/synthetic-content.ndjson data/synthetic-content.failures.ndjson",
    "summarize:synthetic": "tsx scripts/summarize-synthetic-data.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^26.1.1",
    "dotenv": "^16.4.7",
    "tsx": "^4.19.3",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.110.0"
  }
}